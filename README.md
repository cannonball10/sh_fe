# sh_fe

Simple framework for a web-based game.

## What is included

- `index.html`: app shell and game canvas
- `styles.css`: basic responsive layout
- `game.js`: tiny game framework (input, scene update/render loop)

## Run locally

Open `index.html` in a browser.
